{
  "name": "build-my-programming-language",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "repl": "bun run index.ts",
    "run": "bun run index.ts",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vitest": "^4.0.17"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "chevrotain": "^11.1.0"
  }
}
